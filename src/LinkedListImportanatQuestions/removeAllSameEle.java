package LinkedListImportanatQuestions;

public class removeAllSameEle {
    public static void remove(Node head){
        if(head==null) return;

    }
    public static void main(String[] args) {
        Node a=new Node(1);
        Node b=new Node(2);
        Node c=new Node(3);
        Node d=new Node(4);
        Node e=new Node(5);

        remove(a);

    }
}
